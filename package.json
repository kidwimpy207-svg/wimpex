{
  "name": "wimpex",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "start:dev": "node server/index.js",
    "test": "node server/tests/unit.test.js",
    "docker:build": "docker build -t wimpex:latest .",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down"
  },
  "dependencies": {
    "nodemailer": "^7.0.11",
    "node-fetch": "^2.6.9"
  }
}
